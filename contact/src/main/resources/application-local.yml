spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost/contacts
    username: root
    password: root
  jpa:
      hibernate-ddl-auto: update
      generate-ddl: true
      show-sql: true